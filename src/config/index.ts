/**
 * 配置模块统一导出
 */

export { GAME_CONFIG } from './game.config';
export { ENV_CONFIG } from './env.config';
export { SCENE_NAMES, GAME_LIST, FILTER_OPTIONS, GAME_TYPES } from './constant';
export { AppConfigManager, appConfig } from './app-config';

export type { HomePageConfig, GameConfig, BannerConfig } from './home.types';
